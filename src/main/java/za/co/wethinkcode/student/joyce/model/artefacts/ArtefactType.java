package za.co.wethinkcode.student.joyce.model.artefacts;

/* Enumerated means that something has a defined set of values it can hold. Think of the days of the week. Unless something drastic happens, we will always have Sunday through Saturday.
   These values could be stored in an enumerated (in Java, we'll use the key word enum) variable.
   They are unchanging and easily accessible.
*/

public enum ArtefactType {
    WEAPON,
    ARMOUR,
    HELM
}
